# Required project metadata
sonar.projectKey=Schema2Script801G1
sonar.projectName=Schema2Script801G1
sonar.projectVersion=1.0

# Source code directories
sonar.sources=src/edu

# Bytecode directories (compiled .class files)
sonar.java.binaries=target/classes

# Libraries or dependencies (if you have external JARs used by the project)
sonar.java.libraries=lib/*.jar

# Java version settings (you are using Java 17)
sonar.java.source=17
sonar.java.target=17

# Encoding of the source files (default is usually UTF-8)
sonar.sourceEncoding=UTF-8

# Optional: Disable quality gate failure for initial testing
sonar.qualitygate.wait=false

sonar.tests=src/test/java